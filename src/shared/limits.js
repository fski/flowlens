// Shared cap constants for HostConfig match array validation.
// Used by: build.mjs, panel.js buildMatch(), sw.js validateIncomingMessage().
const MAX_MATCH_ARRAY = 80;
const MAX_MATCH_STRING = 256;
